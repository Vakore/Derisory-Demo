//var collectableImgs = {
//"sword":{x:1,y:1,w:5,h:11,shiftX:0,shiftY:0},
//};

var pickup = function(x, y, sprite, required) {
    this.x = x;
    this.y = y;
    this.sprite = sprite;
    //What is required in order for it to not be visible
    this.required = required;
    this.immuneTimers = [-10, -10];
    this.inWater = false;
};

pickup.prototype.run = function() {
    image("collectableSprites", collectableImgs[this.sprite], this.x, this.y);
};